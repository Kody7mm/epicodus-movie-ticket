function Ticket(movie, timeOfDay, age) {
  this.movie = movie;
  this.timeOfDay = timeOfDay;
  this.age = age;
}

function Movie(title, rating, status) {
  this.title = title;
  this.rating = rating;
  this.status = status;
}
